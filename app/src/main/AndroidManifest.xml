<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jixstreet.kolla">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".KollaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name=".intro.IntroActivity_"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity_"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".booking.room.RoomListActivity_"
            android:label="@string/title_activity_room_list"
            android:theme="@style/AppTheme" />
        <activity android:name=".booking.BookingOptionActivity_" />
        <activity
            android:name=".booking.room.detail.RoomDetailActivity_"
            android:label="@string/title_activity_room_detail"
            android:theme="@style/AppTheme" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".booking.room.payment.OtherPaymentActivity_"
            android:label="@string/title_activity_other_payment"
            android:theme="@style/AppTheme" />
        <activity android:name=".booking.BookingConfirmationActivity_" />
        <activity android:name=".booking.BookingSuccessActivity_" />
        <activity android:name=".booking.BookingSizeActivity_" />
        <activity android:name=".booking.office.SurveyRequestOptionActivity_" />
        <activity
            android:name="com.msaku.library.OTPBrowser"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />

    </application>

</manifest>